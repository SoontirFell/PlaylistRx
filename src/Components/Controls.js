import React from 'react';

export default (props) => {
    return (
        <div  className="playlistTrack">
            <input type="checkbox" checked />
            <a></a>
        </div>
    )
}